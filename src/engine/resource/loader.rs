use std::path::{
    Path,
    PathBuf
};

pub struct Loader {
    root_path: PathBuf,
}

impl Loader {
    fn asset_name_to_path(asset_type_dir: &str, asset_name: &str, asset_ext: &str){
        
    }
}